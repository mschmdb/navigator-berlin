node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# Paraglide
src/lib/paraglide
project.inlang/cache/

# BMAD intermediates (DO NOT ignore _bmad/, skill source is versioned)
_bmad-output/
_user-input/

# Build pipeline cache (Story 1.3)
.cache/

# Story 2.6: OG-Image-Pipeline build outputs (Karten-Snapshots + Satori-Cards)
# Build-Time-erzeugt via `pnpm og:all`, NICHT committed.
# `static/og-default.png` (Story 1.20 Adress-Fallback) bleibt versioniert.
/static/og/snapshots/
/static/og/bezirk/
/static/og/kiez/
/static/og/layer/

# Claude Code session transcripts (PII)
.claude/projects/
